{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ReBoot",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app/main.py",
            "console": "integratedTerminal",
            "env": {
                "REBOOT_WINDOW_MODE": "windowed"
            }
        }
    ]
}
